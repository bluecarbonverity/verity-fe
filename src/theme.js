import { createTheme } from '@mui/material/styles'

// TODO: add full color palette here when available
const theme = createTheme({
  palette: {
    primary: {
      main: '#0066CC',
      constrastText: '#FFFFFF',
    },
    secondary: {
      main: '#111827',
      constrastText: '#FFFFFF',
    },
  },
})

export default theme
